# Project Notes (Takao)

- Scope: main engine (core loop, storytellers, AI controllers, utilities).
- Entry: `src/index.ts`; see `src/core` and `src/ai` for primary logic.
- Tests: `npm test --workspace=Takao` (Vitest).
- Build: `npm run build --workspace=Takao` (depends on Maya build for renderer output).
